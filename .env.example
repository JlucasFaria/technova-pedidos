# Porta em que a API sera exposta
PORT=3000

# Versao exibida no endpoint /api/health
APP_VERSION=1.0.0

# String de conexao do PostgreSQL.
# Quando ausente, a aplicacao usa o armazenamento em memoria (modo desenvolvimento/testes).
DATABASE_URL=postgres://technova:technova123@localhost:5432/pedidos
